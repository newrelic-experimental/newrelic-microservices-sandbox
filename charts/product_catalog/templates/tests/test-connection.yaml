apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "product_catalog.fullname" . }}-test-connection"
  labels:
    {{- include "product_catalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "product_catalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
