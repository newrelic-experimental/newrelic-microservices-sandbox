apiVersion: v1
kind: Service
metadata:
  name: {{ include "product_catalog.fullname" . }}
  labels:
    {{- include "product_catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "product_catalog.selectorLabels" . | nindent 4 }}
