apiVersion: v1
kind: Service
metadata:
  name: {{ include "superheroes.fullname" . }}
  labels:
    {{- include "superheroes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "superheroes.selectorLabels" . | nindent 4 }}
